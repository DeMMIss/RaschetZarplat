<Window x:Class="РасчетВыплатЗарплаты.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="РасчетВыплатЗарплаты" 
        Height="700" Width="1000" MinWidth="950"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="15,10">
                <Button Content="📁 Загрузить конфиг" Style="{StaticResource SecondaryButtonStyle}" 
                        Click="LoadConfig_Click" Margin="0,0,10,0" Padding="12,8"
                        ToolTip="Загрузить конфигурацию из JSON файла"/>
                <Button Content="💾 Сохранить конфиг" Style="{StaticResource SecondaryButtonStyle}" 
                        Click="SaveConfig_Click" Padding="12,8"
                        ToolTip="Сохранить текущую конфигурацию в JSON файл"/>
            </StackPanel>
        </Border>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
            <StackPanel>
                <GroupBox Header="💰 Зарплата">
                    <Grid Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300" MinWidth="280"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Label Grid.Row="0" Grid.Column="0" Content="Месячный оклад:"/>
                        <TextBox x:Name="MonthlySalaryTextBox" Grid.Row="0" Grid.Column="1" Margin="5,0"
                                 ToolTip="Месячный оклад в рублях"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" Content="Тип оклада:"/>
                        <ComboBox x:Name="SalaryTypeComboBox" Grid.Row="1" Grid.Column="1" Margin="5,0"
                                  ToolTip="Net - зарплата на руки, Gross - до вычета НДФЛ">
                            <ComboBoxItem Content="Net (на руки)" IsSelected="True"/>
                            <ComboBoxItem Content="Gross (до НДФЛ)"/>
                        </ComboBox>
                        
                        <Label Grid.Row="2" Grid.Column="0" Content="День выплаты аванса:"/>
                        <TextBox x:Name="AdvancePayDayTextBox" Grid.Row="2" Grid.Column="1" Margin="5,0"
                                 ToolTip="Число месяца, когда выплачивается аванс (например, 20)"/>
                        
                        <Label Grid.Row="3" Grid.Column="0" Content="День выплаты расчёта:"/>
                        <TextBox x:Name="SettlementPayDayTextBox" Grid.Row="3" Grid.Column="1" Margin="5,0"
                                 ToolTip="Число месяца, когда выплачивается расчёт (например, 5)"/>
                        
                        <Label Grid.Row="4" Grid.Column="0" Content="Оклад на испытательном сроке:"/>
                        <TextBox x:Name="ProbationSalaryTextBox" Grid.Row="4" Grid.Column="1" Margin="5,0"
                                 ToolTip="Оклад во время испытательного срока (необязательно)"/>
                        
                        <Label Grid.Row="5" Grid.Column="0" Content="Длительность испытательного срока (мес.):"/>
                        <TextBox x:Name="ProbationPeriodTextBox" Grid.Row="5" Grid.Column="1" Margin="5,0"
                                 ToolTip="Продолжительность испытательного срока в месяцах (необязательно)"/>
                    </Grid>
                </GroupBox>
                
                <GroupBox Header="📈 Индексация">
                    <Grid Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300" MinWidth="280"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Label Grid.Row="0" Grid.Column="0" Content="Дата найма:"/>
                        <DatePicker x:Name="HireDatePicker" Grid.Row="0" Grid.Column="1" Margin="5,0"
                                    ToolTip="Дата начала работы"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" Content="Базовая зарплата (net):"/>
                        <TextBox x:Name="BaseSalaryTextBox" Grid.Row="1" Grid.Column="1" Margin="5,0"
                                 ToolTip="Базовая зарплата на момент найма (net, необязательно)"/>
                        
                        <Label Grid.Row="2" Grid.Column="0" Content="События индексации:" VerticalAlignment="Top" Margin="0,8,0,0"/>
                        <DataGrid x:Name="IndexationDataGrid" Grid.Row="2" Grid.Column="1" Margin="5,0" 
                                  AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True" Height="150"
                                  ToolTip="Список индексаций зарплаты. Укажите дату, процент и отметьте, была ли индексация проведена">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Дата" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Date, StringFormat='dd.MM.yyyy'}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <DatePicker SelectedDate="{Binding Date}" BorderThickness="0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Процент" Binding="{Binding Percent}" Width="*"/>
                                <DataGridCheckBoxColumn Header="Проведена" Binding="{Binding IsPerformed}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>
                
                <GroupBox Header="⚙️ Дополнительно">
                    <StackPanel Margin="15">
                        <Expander x:Name="HolidayWorkExpander" Header="🎉 Работа в праздничные и выходные дни" IsExpanded="False" 
                                  Margin="0,0,0,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" 
                                  Background="{StaticResource CardBackgroundColor}" Padding="12">
                            <StackPanel Margin="10,15,0,0">
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="300" MinWidth="280"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Content="Метод расчёта среднесуточного оклада:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <ComboBox x:Name="HolidayWorkDailyRateMethodComboBox" Grid.Column="1" SelectedIndex="0"
                                              ToolTip="Выберите метод расчёта среднесуточного оклада для работы в праздники">
                                        <ComboBoxItem Content="По количеству рабочих дней в месяце расчёта" Tag="MonthlyWorkDays" IsSelected="True"/>
                                        <ComboBoxItem Content="По среднемесячному количеству рабочих дней в году расчёта" Tag="AverageMonthlyWorkDaysPerYear"/>
                                    </ComboBox>
                                </Grid>
                                
                                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="{StaticResource CardBackgroundColor}" 
                                        CornerRadius="4" Padding="12" Margin="0,0">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Column="0" Content="Дата:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <DatePicker x:Name="HolidayWorkDatePicker" Grid.Column="1" Margin="0,0,10,0"
                                                        SelectedDate="{x:Static sys:DateTime.Today}"/>
                                            <Button Grid.Column="2" Content="➕ Добавить" Style="{StaticResource SecondaryButtonStyle}" 
                                                    Padding="12,8" Click="AddHolidayWork_Click"
                                                    ToolTip="Добавить дату работы в праздник"/>
                                        </Grid>
                                        <ListBox x:Name="HolidayWorkListBox" MaxHeight="200" 
                                                 BorderThickness="0" Background="Transparent">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="8,6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding Date, StringFormat='dd.MM.yyyy'}" 
                                                                       VerticalAlignment="Center" FontSize="13"/>
                                                            <Button Grid.Column="1" Content="❌" Style="{StaticResource SecondaryButtonStyle}" 
                                                                    Padding="6,4" Margin="5,0,0,0" Click="RemoveHolidayWork_Click"
                                                                    Tag="{Binding}" ToolTip="Удалить"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Expander>
                        
                        <Expander x:Name="SickLeaveExpander" Header="🏥 Больничные" IsExpanded="False" 
                                  Margin="0,0,0,10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" 
                                  Background="{StaticResource CardBackgroundColor}" Padding="12">
                            <StackPanel Margin="10,15,0,0">
                                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="{StaticResource CardBackgroundColor}" 
                                        CornerRadius="4" Padding="12" Margin="0,0">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Column="0" Content="С:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <DatePicker x:Name="SickLeaveFromPicker" Grid.Column="1" Margin="0,0,10,0"
                                                        SelectedDate="{x:Static sys:DateTime.Today}"/>
                                            <Label Grid.Column="2" Content="По:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <DatePicker x:Name="SickLeaveToPicker" Grid.Column="3" Margin="0,0,10,0"
                                                        SelectedDate="{x:Static sys:DateTime.Today}"/>
                                        </Grid>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Column="0" Content="Сумма:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBox x:Name="SickLeaveAmountTextBox" Grid.Column="1" Margin="0,0,10,0"
                                                     Text="0" ToolTip="Выплаченная сумма за больничный"/>
                                            <Button Grid.Column="2" Content="➕ Добавить" Style="{StaticResource SecondaryButtonStyle}" 
                                                    Padding="12,8" Click="AddSickLeave_Click"
                                                    ToolTip="Добавить период больничного"/>
                                        </Grid>
                                        <ListBox x:Name="SickLeavesListBox" MaxHeight="200" 
                                                 BorderThickness="0" Background="Transparent">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="8,6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="С:" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="13"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding From, StringFormat='dd.MM.yyyy'}" 
                                                                       VerticalAlignment="Center" FontSize="13" Margin="0,0,15,0"/>
                                                            <TextBlock Grid.Column="2" Text="По:" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="13"/>
                                                            <TextBlock Grid.Column="3" Text="{Binding To, StringFormat='dd.MM.yyyy'}" 
                                                                       VerticalAlignment="Center" FontSize="13" Margin="0,0,15,0"/>
                                                            <TextBlock Grid.Column="4" Text="{Binding Amount, StringFormat='Сумма: {0:N2} руб.'}" 
                                                                       VerticalAlignment="Center" FontSize="13" Margin="0,0,10,0"/>
                                                            <Button Grid.Column="5" Content="❌" Style="{StaticResource SecondaryButtonStyle}" 
                                                                    Padding="6,4" Click="RemoveSickLeave_Click"
                                                                    Tag="{Binding}" ToolTip="Удалить"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Expander>
                        
                        <Expander x:Name="VacationExpander" Header="🏖️ Отпускные" IsExpanded="False"
                                  BorderBrush="{StaticResource BorderColor}" BorderThickness="1" 
                                  Background="{StaticResource CardBackgroundColor}" Padding="12">
                            <StackPanel Margin="10,15,0,0">
                                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="{StaticResource CardBackgroundColor}" 
                                        CornerRadius="4" Padding="12" Margin="0,0">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Column="0" Content="С:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <DatePicker x:Name="VacationFromPicker" Grid.Column="1" Margin="0,0,10,0"
                                                        SelectedDate="{x:Static sys:DateTime.Today}"/>
                                            <Label Grid.Column="2" Content="По:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <DatePicker x:Name="VacationToPicker" Grid.Column="3" Margin="0,0,10,0"
                                                        SelectedDate="{x:Static sys:DateTime.Today}"/>
                                        </Grid>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Grid.Column="0" Content="Сумма:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBox x:Name="VacationAmountTextBox" Grid.Column="1" Margin="0,0,10,0"
                                                     Text="0" ToolTip="Выплаченная сумма за отпуск"/>
                                            <Button Grid.Column="2" Content="➕ Добавить" Style="{StaticResource SecondaryButtonStyle}" 
                                                    Padding="12,8" Click="AddVacation_Click"
                                                    ToolTip="Добавить период отпуска"/>
                                        </Grid>
                                        <ListBox x:Name="VacationsListBox" MaxHeight="200" 
                                                 BorderThickness="0" Background="Transparent">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,0,1" Padding="8,6">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="С:" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="13"/>
                                                            <TextBlock Grid.Column="1" Text="{Binding From, StringFormat='dd.MM.yyyy'}" 
                                                                       VerticalAlignment="Center" FontSize="13" Margin="0,0,15,0"/>
                                                            <TextBlock Grid.Column="2" Text="По:" Margin="0,0,5,0" VerticalAlignment="Center" FontSize="13"/>
                                                            <TextBlock Grid.Column="3" Text="{Binding To, StringFormat='dd.MM.yyyy'}" 
                                                                       VerticalAlignment="Center" FontSize="13" Margin="0,0,15,0"/>
                                                            <TextBlock Grid.Column="4" Text="{Binding Amount, StringFormat='Сумма: {0:N2} руб.'}" 
                                                                       VerticalAlignment="Center" FontSize="13" Margin="0,0,10,0"/>
                                                            <Button Grid.Column="5" Content="❌" Style="{StaticResource SecondaryButtonStyle}" 
                                                                    Padding="6,4" Click="RemoveVacation_Click"
                                                                    Tag="{Binding}" ToolTip="Удалить"/>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Expander>
                    </StackPanel>
                </GroupBox>
                
                <GroupBox Header="📋 Опциональные расчёты" Margin="0,15,0,0">
                    <StackPanel Margin="15">
                        <GroupBox x:Name="UnusedVacationGroupBox" Header="🎯 Компенсация за неиспользованный отпуск" Margin="0,0,0,0" FontSize="13">
                            <StackPanel Margin="10">
                                <CheckBox x:Name="CalculateUnusedVacationCompensationCheckBox" Content="Рассчитать компенсацию за неиспользованный отпуск при увольнении" 
                                          Margin="0,0,0,15" Checked="CalculateUnusedVacationCompensationCheckBox_CheckedChanged" 
                                          Unchecked="CalculateUnusedVacationCompensationCheckBox_CheckedChanged"
                                          ToolTip="Для расчета будет использована дата увольнения (по умолчанию - дата расчета)"/>
                                
                                <Grid x:Name="DismissalDateGrid" Margin="0,0,0,0" Visibility="Collapsed">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="300" MinWidth="280"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Label Grid.Column="0" Content="Дата увольнения:"/>
                                    <DatePicker x:Name="DismissalDatePicker" Grid.Column="1" Margin="5,0"
                                                ToolTip="Дата увольнения для расчета компенсации за неиспользованный отпуск (по умолчанию - дата расчета)"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Column="0" Content="Дата расчёта:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="CalculationDatePicker" Grid.Column="1" Margin="0,0,15,0"
                            ToolTip="Дата, на которую выполняется расчёт задолженности"
                            SelectedDateChanged="CalculationDatePicker_SelectedDateChanged"/>
                <Button x:Name="CalculateButton" Grid.Column="2" Content="🔢 Рассчитать" Style="{StaticResource PrimaryButtonStyle}" 
                        Click="CalculateButton_Click"
                        ToolTip="Выполнить расчёт задолженности по зарплате"/>
            </Grid>
        </Border>
    </Grid>
</Window>
